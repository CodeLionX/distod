# Configuration variables for the FASTOD spark experiments
---
component: "fastod-spark"
src_path: "../{{ component }}"
deployment_path: "~/distod/{{ component }}"
data_deployment_path: "{{ deployment_path }}/data"
lock_file: "/var/lock/{{ component }}-{{ experiment }}.lock"
