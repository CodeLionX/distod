# ignore data (too large: a couple hundrets of MB)
data
fastod-spark/data

# ignore results folder
results

# ignore our own artefact, but include the other ones
!fastod/fastod.jar
!fastod-spark/libs/*
!fastod-spark/distributed-fastod.jar

**/distod.jar
