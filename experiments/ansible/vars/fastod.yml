# Configuration variables for the FASTOD (single-node) experiments
---
component: "fastod"
src_path: "../{{ component }}"
deployment_path: "~/distod/{{ component }}"
data_deployment_path: "~/distod/data"
lock_file: "/var/lock/{{ component }}-{{ experiment }}.lock"
